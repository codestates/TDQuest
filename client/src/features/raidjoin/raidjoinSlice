import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";
import { TDQuestAPI } from "../../API/tdquestAPI";

export interface damageLogInfo {
  log : string;
  created_at : string; 
  updated_at : string;
  user_id : string;
  raid_id : string;
}

export const initialState: damageLogInfo = {
  log : "",
  created_at : "", 
  updated_at : "",
  user_id : "",
  raid_id : "",
};